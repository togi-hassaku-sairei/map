# 冨木八朔祭礼デジタルマップ ＜引き継ぎマニュアル＞

このシステムは、祭りの**キリコ・神輿等の位置を、見物客がスマホの地図でリアルタイムに見られる**ようにするものです。
**プログラミングの知識がなくても、毎年これだけ読めば運用・引き継ぎができます。**

まずは全体像だけつかんでください。細かい操作は後ろの章にあります。

---

## いちばん大事なこと（3行まとめ）

1. **位置は、キリコ・神輿等に載せたGPS端末から自動で届きます**（スマホで送る操作は不要）。
2. **毎年の設定は「スプレッドシートのMaster」と「管理者サイト」でできます**（プログラムは触りません）。
3. **祭りが終わったら「自動取得のスイッチ（トリガー）」を切ります**（理由は第7章）。

---

## 0. 使う4つのURL（ブックマーク推奨）

| 用途 | だれが使う | URL |
|---|---|---|
| 地図を見る（一般公開） | 見物客・全員 | https://togi-hassaku-sairei.github.io/map/ |
| 管理（参加・並び順・紹介文・画像・削除） | 本部の担当者 | https://togi-hassaku-sairei.github.io/map/admin/ |
| 過去のルートを見る | 主催者・振り返り | https://togi-hassaku-sairei.github.io/map/history/ |
| 緊急スマホ送信（予備） | 端末が足りない時だけ | https://togi-hassaku-sairei.github.io/map/sender/ |

- **見物客に案内するのは「地図を見る」URLだけ**です。ほかは運営内だけで共有してください。
- 各ページへは、掲示やチラシに**QRコード**を貼ると便利です（「地図を見る」URLでQRを作成）。

---

## 1. このシステムの仕組み（ざっくり）

```
[キリコ・神輿等に載せたGPS端末]
        ↓ 位置を送信
[レンタル事業者のシステム（おわっちゃ）]
        ↓ インターネットで公開（JSON）
[Googleのプログラム（GAS）]  ← 1分ごとに位置を取りに行く
        ↓ スプレッドシートに保存
[Googleスプレッドシート]（最新位置・全履歴・設定）
        ↓
[地図ページ（GitHub）]  ← 見物客のスマホに地図で表示
```

ポイントは、**GPSの送信は事業者側が自動で行い、こちらは「1分ごとに取りに行って地図に出す」だけ**ということです。
費用は**0円**（無料の仕組みだけで動いています。第7章で説明します）。

---

## 2. 見物客が使える機能（知っておくと案内できます）

地図ページ（一般公開URL）では、見物客が次のことをできます。

- **キリコ・神輿等の現在地**を地図で見る（地区の紋・アイコンで色分け）
- **「🔍 キリコ・神輿等検索」**で地区名を探して、その位置へ移動
- **動き出すと波紋**が出る／**移動中・停止中・進行方向**が分かる
- **「📍 現在地」ボタン**で、自分の今いる場所を地図に表示（自分のスマホだけに表示。他人には見えません）
- **「🗺️」ボタン**で、地図を「標準地図 ↔ 航空写真」に切り替え
- アイコンをタップすると、**紹介文と写真**（設定していれば）が出る

> これらは自動で動きます。特別な設定は不要です。

---

## 3. 毎年の設定①：スプレッドシートの「Master」

担当地区・神輿名・参加・端末の割り当てが変わる年は、**スプレッドシートの「Master」シート**を書き換えます。
（プログラムは触りません。書き換えると自動で反映されます）

「Master」シートの列の意味：

| 列 | 意味 | 変更するとき |
|---|---|---|
| 神輿ID | システム内の管理番号（m01, g01 など） | **触らない**（プログラムと対応しているため） |
| 神輿名 | 表示名 | 名前が変わったら |
| 担当地区 | 地区名 | 変わったら |
| 参加 | TRUE=参加／FALSE=不参加 | その年に出ない地区はFALSE |
| 表示順 | 一覧に並ぶ順番 | 並び替えたいとき |
| **APIのID** | **その神輿に載せた端末の番号** | **端末を載せ替えた年（毎年ここが要）** |
| 紹介文 | 地図に出る説明文 | 管理者サイトで編集推奨（第5章） |
| 画像URL | 地図に出る写真 | 管理者サイトで編集推奨（第5章） |

### ★「APIのID」がいちばん大事

「APIのID」は、**どの端末が、どのキリコ・神輿等か**を対応づける列です。
- ここが空だと、その神輿は**地図に出ません**。
- 端末は使い回されるので、**当日、載せた端末の番号を入れます**（やり方は第6章）。
- **できれば `device_id`（長い番号＝IMEI）**を入れてください。端末固有で変わらないため、毎年の付け替えでズレません。

> 神輿の色や紋の画像そのものを変えたいときだけ、分かる人に相談してください（設定ファイル `config.js` の変更が必要です）。

---

## 4. 毎年の設定②：管理者サイト（参加・並び順・削除）

管理者サイト（/admin/）でも、パソコン操作だけで設定できます（スプレッドシートを開かなくてもOK）。

1. 管理者サイトを開く → **管理パスワード**を入力
2. できること：
   - **参加/不参加の切替**（チェックのON/OFF）
   - **表示順の並べ替え**（▲▼）
   - **紹介文・画像の編集**（第5章）
   - **誤って入ったデータの削除**
3. 変更したら**「保存」**を押す（地図に反映されます）

> ただし「APIのID」の設定だけは管理者サイトにありません。**端末の割り当て（APIのID）はスプレッドシートのMasterで行います**（第6章）。

---

## 5. ★紹介文と写真を出す（管理者サイトでできます）

各キリコ・神輿等の**紹介文と写真**を、地図のポップアップに表示できます。
**GitHubを触らず、管理者サイトだけで編集・変更できます。** ここが毎年いちばん使う機能です。

### 5-1. 紹介文の入れ方

1. 管理者サイト（/admin/）を開く → 管理パスワードを入力
2. 「紹介文・画像の編集」欄で、各地区の**紹介文**を入力
3. **「紹介文・画像を保存」**を押す → 地図のポップアップに反映

### 5-2. 写真の入れ方（Googleドライブを使います）

写真は「どこかに置いたURL」を貼る方式です。**Googleドライブ**に置くのが簡単です。

**手順：**

1. **Googleドライブに写真をアップロード**する（主催者のGoogleアカウントのドライブへ）
2. その写真を右クリック →**「共有」**
3. 「一般的なアクセス」を**「リンクを知っている全員」**にし、権限を**「閲覧者」**にする
   - ★ここが**非公開のままだと、見物客の画面に写真が出ません**。必ず「全員・閲覧者」にしてください。
4. **「リンクをコピー」**を押す
5. 管理者サイトの「画像URL」欄に、コピーしたリンクを**貼り付ける**
   - ドライブのリンクは自動で「表示用」に変換されます（そのまま貼るだけでOK）
   - 貼るとその場で**プレビュー**が出ます
6. **「紹介文・画像を保存」**を押す → 地図に写真が表示される

> すでにネット上に公開されている画像URL（`https://…/○○.jpg` など）があれば、それをそのまま貼っても表示されます。

### 5-3. うまく写真が出ないとき

- ドライブの共有設定が**「リンクを知っている全員・閲覧者」**になっているか確認（いちばん多い原因）
- 別の写真URLで試す／プレビューが出ているか確認

---

## 6. 当日の流れ

### 準備（前日〜当日朝）
1. GPS端末（12台）を充電・準備（事業者の指示に従う）
2. 「Master」シートの**神輿名・担当地区・参加**が今年の内容か確認
3. できれば、紹介文・写真も事前に管理者サイトで設定（第5章）

### 当日（各キリコ・神輿等に端末を載せてから）
1. 各キリコ・神輿等に**GPS端末を取り付ける**（電源ON）。**どの端末をどの神輿に載せたかメモ**を取る
2. **GASで `listDevices` を実行**して、今つながっている端末の一覧（番号）を確認（やり方は第8章）
3. スプレッドシートの「Master」の**「APIのID」列**に、その神輿に載せた端末の番号（できれば `device_id`）を入れる
4. **1分待つ** → 地図で全台が正しい位置に出るか確認
5. ずれていたら「APIのID」を直す（何度でも直せます）

### 終了後
1. **GASの自動取得を止める**（トリガーを削除。第7章）
2. 端末を回収し、事業者へ返却

> 地図は開いたままで**自動更新**されます（30秒ごと）。見物客は再読み込み不要です。

---

## 7. ★GASの操作と「なぜ止めるのか」（来年につなぐ大事な話）

「GAS」は、**1分ごとにGPSの位置を取りに行って、スプレッドシートに保存するプログラム**です。
ここでは、**それぞれの操作が何をするものか・いつ実行するか**を説明します。むずかしく考えず、「スイッチのON/OFF」だと思ってください。

### 7-1. GASの開き方

主催者のGoogleアカウントで、**該当のスプレッドシートを開く → メニュー「拡張機能」→「Apps Script」**。
これでプログラムの画面が開きます。上のほうにある**関数を選ぶ欄**と**「実行」ボタン**を使います。

### 7-2. 関数（＝操作）の意味と、いつ実行するか

| 操作（関数名） | 何をするもの | いつ実行する |
|---|---|---|
| **setupSheets** | 保存用のシート（表）を作る**最初の準備** | **最初の1回だけ**（新しく作り直したとき） |
| **listDevices** | 今つながっている端末の番号を一覧表示する | **当日、APIのIDを調べるとき** |
| **testApiOnce** | 位置データがちゃんと取れるか**お試しで1回**確認 | 動作確認したいとき（いつでも可） |
| **pullFromApi** | 位置を**今すぐ1回だけ**取りに行く | 手動で1回だけ試したいとき |
| **installTrigger** | 「**1分ごとに自動で取りに行く**」スイッチをONにする | **当日（または前日）、取得を始めるとき** |
| （トリガー削除） | その自動取得スイッチをOFFにする | **祭りが終わったら**（第7-4） |

> **「自動で動かし続けたい」＝installTrigger を実行**、**「1回だけ試したい」＝pullFromApi を実行**、と覚えれば十分です。

### 7-3. なぜ「無料」なのか（お金はかかりません）

- このGASは**無料**で、そもそも**お金を請求する仕組みがありません**（クレジットカードも登録していません）。
- 使える量に**上限**（＝1日に自動で動ける合計時間。目安90分）がありますが、祭りの2日間（1分ごと・1日十数時間）でも**上限内**に収まります。
- **万一上限を超えても、課金は絶対にされません。**「その日はそれ以上動かない（停止）」だけで、翌日リセットされます。

> つまり「**請求が来る心配は不要**」です。心配するのは「動くか止まるか」だけです。

### 7-4. ★なぜ祭りが終わったら「止める」のか

祭りが終わると、GPS端末が止まって位置を返さなくなります。
このとき**自動取得（トリガー）を動かしたままにすると**、

- 1分ごとに「取りに行く → 端末が無い → エラー」をくり返し、**ErrorLog（エラー記録）にエラーが延々と溜まります**
- 無料の使える量も、**意味なく少しずつ消費**します（※課金はされません）

害はありませんが、**きれいに保つため・無駄をなくすため**に、祭りが終わったら止めます。

**止め方（自動取得のスイッチをOFFにする）：**

1. GASの画面の左メニューにある**時計のアイコン（「トリガー」）**をクリック
2. `pullFromApi` の行の右端**「⋮」→「トリガーを削除」**
3. 一覧から消えたら停止完了（数分後、ErrorLogに新しい行が増えなければOK）

**来年また使うとき：** GASの画面で **installTrigger を実行**すれば、自動取得が再開します。

### 7-5. プログラム（コード）を直したときだけ：再デプロイ

紹介文・画像・参加設定などは**保存するだけで反映**され、再デプロイは不要です。
**プログラム（Code.gs）そのものを直したときだけ**、次の操作が必要です。

- 「デプロイ」→「デプロイを管理」→ 鉛筆マーク → バージョン「**新バージョン**」→「デプロイ」

> ふだんの運用（Master編集・管理者サイト）では、この操作は不要です。

---

## 8. 端末の番号を調べる（listDevices）

当日、「APIのID」に何を入れるか調べる操作です。

1. GASの画面を開く（第7-1）
2. 関数の欄で **`listDevices`** を選ぶ → **「実行」**
3. 下に出る**「実行ログ」**に、今つながっている端末が一覧表示されます
   （`id`＝短い番号、`device_id`＝長い番号、名前、位置、更新時刻）
4. 自分たちの端末を見分けて、その **`device_id`（長い番号）**を、Masterの「APIのID」に転記

> このAPIは**複数の祭りが共用**しているため、一覧には他の地域の端末も出ます。**自分たちの端末だけ**をMasterに登録してください（登録した端末だけが地図に出ます）。

---

## 9. 緊急用：スマホでの位置送信（予備）

GPS端末が**足りない・壊れた**ときの**予備**として、スマホのブラウザから位置を送れます（/sender/）。

- 神輿を選ぶ → **書き込みキー**を入力 → 「送信開始」
- **⚠ 画面を消す・別アプリに切り替える・スリープにすると止まります。**「充電しながら・画面をつけたまま」使ってください。
- ふだんは使いません。あくまで**端末不足時の穴埋め**です。

---

## 10. 過去のルートを見る（記録URL）

祭りが終わったら、翌年以降いつでも過去の巡行を振り返れます。

1. 記録URL（/history/）を開く
2. 上の「**年度**」で見たい年を選ぶ → ルートが色分けで表示
3. 「**神輿**」で1基だけに絞れる
4. 「**▶ 再生**」で、その年の巡行が地図上で動いて再現され、**通った軌跡が伸びていきます**
5. 「**📊 集計**」で、神輿ごとの移動距離・所要時間・平均速度
6. 「**🗺️**」で標準地図↔航空写真の切り替え

> **Historyシート（過去の全記録）は絶対に消さないでください**。この機能の元データです。

---

## 11. 困ったときの早見表

| 症状 | 対処 |
|---|---|
| 特定の神輿が地図に出ない | Masterの「APIのID」が正しく入っているか（空・誤り）を確認。参加がTRUEか確認 |
| 全部出ない | GASの自動取得（トリガー）が動いているか確認。ErrorLogにエラーが無いか確認 |
| 位置がずれる／飛ぶ | GPSの一時的な誤差。数分で戻ることが多い。端末は空が見える向きに |
| 「通信断」ではないが古い位置のまま | その端末が止まっている可能性。電池・電波を確認 |
| 写真が出ない | Googleドライブの共有を「リンクを知っている全員・閲覧者」にする（第5-3） |
| 紹介文・写真を変えたい | 管理者サイトの「紹介文・画像の編集」→保存（第5章） |
| ErrorLogにエラーが溜まる（祭り後） | 端末が止まっているため。自動取得トリガーを削除（第7-4） |
| 緊急送信で「別の端末が使用中」 | その神輿は別スマホが送信中（二重送信防止の正常動作） |

---

## 12. GitHub（地図ページの置き場所）で、主催者ができること

GitHub（ギットハブ）は、**地図ページそのもの（見た目・ファイル）を置いてある場所**です。
ふだんの運用（参加設定・紹介文・写真）は管理者サイトやMasterでできるので、**GitHubを触る場面は多くありません**。
ここでは、**主催者側で起こりがちなこと**に絞って、できることだけ説明します（プログラムの中身の改修は、分かる人に相談してください）。

### 12-0. 開き方・基本

- ログイン：`https://github.com/` に、引き継いだGitHubアカウントでログイン
- リポジトリ（ファイル一式の置き場所）：**togi-hassaku-sairei / map**
- ここに `index.html`（地図ページ）や `img`（画像）、`js`（設定）などのファイルが入っています。
- ファイルを変えたら、最後に**「Commit changes」（保存）**を押すと、数十秒〜数分で公開ページに反映されます。

> ★鉄則：**分かる範囲だけ触る。**下記に書いていないファイル（プログラム本体）は、分かる人以外いじらないでください。

### 12-1. 参加する地区・神輿が増えた/減ったとき

**減った（不参加になった）だけ**なら、GitHubは不要です。
→ **管理者サイトで「参加」のチェックを外す**、またはMasterで参加をFALSEにするだけ（第3・4章）。

**新しい地区・神輿が増えたとき**は、GitHubの設定ファイルに1行足す必要があります。少し手順が要るので、以下の通りに行うか、分かる人に依頼してください。

1. リポジトリの **`js` フォルダ → `config.js`** を開く
2. 右上の**鉛筆マーク（Edit）**を押す
3. 神輿の一覧（`MIKOSHI` という部分）の最後の行の**末尾にカンマ（ ,）**を足し、次の1行を追加します（例）：
   ```
   { id: "m15", name: "○○地区", color: "#8E24AA", icon: "img/m15.png?v=3" }
   ```
   - `id`：**他と重複しない新しい番号**（例：m15）。この番号は以後変えない
   - `name`：表示名
   - `color`：一覧の丸ドット・波紋・軌跡の色（他と被らない色。例は紫）
   - `icon`：紋の画像ファイル名（下記12-2で画像を用意）
4. **「Commit changes」**で保存
5. **画像ファイル（例 `m15.png`）を `img` フォルダにアップロード**（12-2参照）
6. **スプレッドシートのMasterにも同じ「神輿ID（m15）」の行を追加**（神輿名・参加・APIのID等）。GitHubとMasterの**両方に同じIDを用意する**のがポイントです

> 「増設」は年に何度もないので、初めてのときは**分かる人と一緒に**行うと安心です。上記さえ守れば主催者側でも対応できます。

### 12-2. 紋（アイコン）の画像を差し替える・追加する

紋の絵柄を変えたい、新しい神輿の紋を足したいときは、`img` フォルダの画像を差し替え・追加します。

1. 画像は**正方形（例：500×500px程度）**の**PNG**で用意（背景は白か透明が見やすい）
2. リポジトリの **`img` フォルダ**を開く
3. **「Add file → Upload files」**で画像をアップロード（**ファイル名は既存に合わせる**。例：`m03.png`）
   - 同じ名前で上げると**差し替え**、新しい名前なら**追加**になります
4. **「Commit changes」**で保存
5. **すぐに反映されない場合**は、`config.js` のその神輿の `icon` の末尾の番号を1つ上げます
   （例：`img/m03.png?v=3` → `img/m03.png?v=4`）。これは「新しい画像を読み直してね」という合図です

> ※ 紹介文のポップアップ写真は、GitHubではなく**管理者サイト＋Googleドライブ**で管理します（第5章）。GitHubの `img` は「紋アイコン」用です。

### 12-3. 神輿名だけ変えたいとき

- **地図の一覧・ポップアップの名前**は、基本 **Master／管理者サイト**で変わります（第3・4章）。まずはそちらで。
- ページのタイトル（画面上部の青いバーの文字）を変えたいときだけ、`index.html` の該当箇所の文字を直します（分かる範囲で。不安なら依頼）。

### 12-4. GitHubアカウントやリポジトリ名が変わったとき（URLが変わる）

主催者交代などで**GitHubアカウントを変えた**場合、公開URLが変わります。

1. 新しいアカウントで、**ファイル一式をアップロード**（前のリポジトリから「Download ZIP」→中身を新リポジトリへ）
2. **Settings → Pages** で公開（Branch: main /(root) → Save）
3. 新しい公開URL（例：`https://○○.github.io/△△/`）を、**見物客への案内・QRコードも差し替え**
4. ※GAS側のURL（config.jsのGAS_URL）は、**Googleアカウントを変えていなければ、そのままでOK**

### 12-5. GitHubで「やらない方がよいこと」

- `index.html` / `css` / `js/map.js` などの**プログラム本体を、分かる人以外が編集しない**
- `gas` フォルダの `Code.gs` は**記録用の控え**です（実際に動くのはスプレッドシート側のGAS）。ここを編集しても動作は変わりません
- フォルダ構成（`index.html` が一番上の階層にある状態）を崩さない

### 12-6. GitHubの困ったとき早見表

| 症状 | 対処 |
|---|---|
| 変更したのにページに反映されない | 数分待って再読み込み。`?v=` の番号を1つ増やすと確実（12-2） |
| 公開ページが404になる | `index.html` が一番上の階層にあるか確認。Settings→Pagesが main /(root) か確認 |
| 新しい神輿が地図に出ない | config.js（GitHub）とMaster（スプレッドシート）の**両方に同じID**があるか、参加TRUEか、APIのID設定済みか |
| 画像が古いまま | `config.js` の `icon` の `?v=番号` を1つ上げてCommit |

---

## 13. やってはいけないこと（データを守るため）

- **Historyシートを削除・並べ替えしない**（過去の全記録。消すと戻せません）
- スプレッドシート自体を削除しない
- Master の「神輿ID」列を書き換えない（プログラムと対応しています）
- GitHubのファイル（プログラム）を、分かる人以外は編集しない
- 管理パスワード・緊急送信キー・各URLを一般公開しない

---

## 14. 重要な保管情報（担当者間で必ず引き継ぐ）

次の「場所」と「合言葉」を、毎年の担当者へ確実に引き継いでください。**これさえあれば翌年もそのまま使えます。**

- **Googleアカウント**（スプレッドシート＝履歴・設定の置き場所）：〔　　　　　　　　　〕
- **GitHubアカウント／リポジトリ**（地図ページの置き場所）：〔 togi-hassaku-sairei / map 〕
- **GASのウェブアプリURL**（…/exec）：`config.js` に設定済み（変更時のみ差し替え）
- **管理パスワード**（管理者サイト用）：〔　　　　　　　　　〕
- **緊急送信キー**（/sender/ 用）：〔　　　　　　　　　〕
- **Googleドライブ**（紹介文の写真の置き場所）：上記Googleアカウント内

---

### 付録：1年の流れ（早見）

1. **祭り前**：Masterで神輿名・担当地区・参加を確認／管理者サイトで紹介文・写真を設定
2. **当日**：端末を載せる → `listDevices` で番号確認 → MasterのAPIのIDに入力 → 地図で確認
   （自動取得が止まっていたら **installTrigger** を実行してON）
3. **祭り後**：自動取得トリガーを**削除**してOFF（第7-4）。Historyは残す
4. **翌年**：1に戻る（installTrigger でON、あとは同じ）

これだけ押さえておけば、毎年同じ仕組みをそのまま使い続けられます。
